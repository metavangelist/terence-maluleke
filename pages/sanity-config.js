/** TerenceART — Sanity project config (public read-only). */
window.SANITY_CONFIG = {
  projectId: "um9my25h",
  dataset: "production",
  apiVersion: "2024-01-01",
  // Live API (not apicdn) so published CMS uploads appear immediately.
  useCdn: false,
};
